name: db-schema-check

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  detect-db-schema-scope:
    runs-on: ubuntu-latest
    outputs:
      run_db_schema_check: ${{ steps.changes.outputs.run_db_schema_check }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect db schema-related changes
        id: changes
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BASE_SHA="${{ github.event.pull_request.base.sha }}"
          else
            BASE_SHA="${{ github.event.before }}"
          fi
          git diff --name-only "$BASE_SHA" "$GITHUB_SHA" > changed_files.txt
          if rg -q '^(db/.*\.sql|\.agent/tests/check-db-schema\.sh|\.github/workflows/db-schema-check\.yml)$' changed_files.txt; then
            echo "run_db_schema_check=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_db_schema_check=false" >> "$GITHUB_OUTPUT"
          fi

  db-schema-check:
    runs-on: ubuntu-latest
    needs: detect-db-schema-scope
    if: needs.detect-db-schema-scope.outputs.run_db_schema_check == 'true'

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: topolactor
          POSTGRES_USER: topolactor
          POSTGRES_PASSWORD: topolactor
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U topolactor -d topolactor"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      POSTGRES_HOST: localhost
      POSTGRES_PORT: "5432"
      POSTGRES_DB: topolactor
      POSTGRES_USER: topolactor
      POSTGRES_PASSWORD: topolactor

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install PostgreSQL client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client
      - name: Run DB schema check
        run: bash .agent/tests/check-db-schema.sh

  db-schema-check-not-required:
    runs-on: ubuntu-latest
    needs: detect-db-schema-scope
    if: needs.detect-db-schema-scope.outputs.run_db_schema_check != 'true'
    steps:
      - name: DB schema check not required for this change
        run: echo "No db/sql scope change detected; db-schema-check marked success by scope gating."
