name: frontend-types

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check-frontend-types:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x

      - name: Run frontend type check
        run: bash .agent/tests/check-frontend-types.sh
