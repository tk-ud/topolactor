Policy-Judgment-Need: NOT_REQUIRED_MECHANICAL_ONLY
Policy-Judgment-Rationale: This change fixes bug(s) in SystemOperationCiScheduler: (a) LogResult now uses LogError for Blocking findings and LogWarning for Gap findings, preserving severity semantics; (b) ParseEnvSeconds/ParseEnvHours add upper-bound guards (3600s / 24h) to prevent TimeSpan overflow; (c) scheduler/*.cs added to test project compile includes for CI compile coverage; (d) cron methods made virtual for testability; (e) tests added. No new policy values, no new DB writes, no new endpoints, no frontend changes. The log severity fix (Blocking->LogError) is a correctness fix to system invariant reporting, not a policy value change. Upper-bound guards on ParseEnv are mechanical safety guards, not topology/runtime policy.
