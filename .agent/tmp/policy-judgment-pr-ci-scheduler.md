Policy-Judgment-Need: NOT_REQUIRED_MECHANICAL_ONLY
Policy-Judgment-Rationale: This change adds SystemOperationCiScheduler (BackgroundService) that wires the three existing cron-triggered inspection methods (InspectHubAttentionContinuityAsync, InspectCurrentRebuildabilityAsync, InspectRegistryContinuityAsync) to a background worker loop. No new policy values, no new DB writes, no new endpoints, no new frontend changes. Environment variable defaults (SYSTEM_CI_STARTUP_DELAY_SECONDS=30, SYSTEM_CI_INTERVAL_HOURS=1) are mechanical scheduler configuration values (startup/interval timing), not runtime policy values that affect topology behavior, recommendation, selection, scoring, or projection. This is a mechanical wiring change with no policy surface impact.
