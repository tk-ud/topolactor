# PR #361 / UI Builder canvas live preview — Owner 検証委託

## 前提
- リポジトリ root: `E:/User/Topolactor/topolactor`
- `deno` が PATH にある Git Bash または同等シェル
- 対象ブランチ: `claude/ui-builder-layout-structure-IShxt`（または該当コミットを含むブランチ）

---

## 1. 自動テスト（コピペ）

```bash
cd /e/User/Topolactor/topolactor

# 今回追加・更新した slice（必須）
deno test frontend/tests/runtimeComponentFactory.test.ts frontend/tests/visualLayoutBuilder.test.ts --allow-read

# UI Builder 関連の既存ガード（推奨）
deno test frontend/tests/adminUxGuard.test.ts frontend/tests/uiBuilderStepper.test.ts --allow-read

# 構造チェック（最後）
bash .agent/tests/check-structure.sh
```

### 合格目安
- `runtimeComponentFactory.test.ts` — 8 tests passed
- `visualLayoutBuilder.test.ts` — 既存 + layout canvas preview 3件が passed
- いずれも `FAILED` / `error: Test failed` が出ないこと

---

## 2. 手動受入（/admin/ui-builder）

1. `deno task dev` で起動し `/admin/ui-builder` を開く
2. パッケージを選び「配置を編集」タブへ
3. パレットから `button.primitive` / `card.primitive` / `box.primitive` / `table.primitive` を canvas に追加
4. 確認:
   - 謎の矩形ラベルだけでなく **実コンポーネント風プレビュー** がノード内に見える
   - `box.primitive` は **Box**（破線コンテナ）で、Card 代用になっていない
   - ドラッグ・リサイズ・インスペクタの x/y/width/height 変更で **プレビュー位置・サイズが即追従**
   - 未接続（draft-only）部品は **「未接続」fallback**（黄色系）で理由が表示される
   - 未対応部品は **「プレビュー未対応」fallback**（琥珀系）で componentKey / code が表示される
5. 「1. プレビュー」ボタンは DB 検証用であり、canvas の見た目更新とは別 — canvas 上の live preview はパレット追加時点で見えること

---

## 3. 報告フォーマット（Owner → Agent）

```
[自動]
runtimeComponentFactory: PASS/FAIL (N passed)
visualLayoutBuilder: PASS/FAIL (N passed)
check-structure: PASS/FAIL

[手動]
button/card/box/table preview: OK/NG
drag-resize追従: OK/NG
draft-only fallback: OK/NG
備考:
```
